{
  "name": "emailmkt",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de campanhas de e-mail marketing — André Araújo Advogados",
  "license": "UNLICENSED",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "synth": "pnpm --filter @emailmkt/infra synth",
    "//verificar": "NÃO renomear para 'ci': o pnpm tem um comando embutido com esse nome, que sombreia o script e roda uma instalação limpa no lugar.",
    "verificar": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm test && pnpm --filter @emailmkt/admin-web build && pnpm synth"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.0",
    "eslint": "^9.17.0",
    "prettier": "^3.4.0",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0",
    "vitest": "^2.1.0"
  }
}
